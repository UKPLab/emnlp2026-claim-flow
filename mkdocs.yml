site_name: arxiv2026_claim_flow
nav:
  - Home: index.md

theme:
  name: material

plugins:
  - search
  - mkdocstrings:
      project_name: arxiv2026_claim_flow
      handlers:
        python:
          options:
            docstring_style: numpy